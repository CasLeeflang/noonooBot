Steps to run the package:
1: unzip the cv2serial zip
2. add the package folder to your workspace /src file
3. Use Colcon to buil the workspace using colcon build --symlink-install
4. run the teleop_twist_keyboard node
5. run cv2serial node
